@model ChatServiceClientMVC.Models.Chat

@{
    ViewBag.Title = "Chat";
}

<h2>Chat</h2>

<fieldset>
    <legend>Chat</legend>
</fieldset>
@foreach (var item in Model.Messages)
{
    Html.Label(item);
}
<p>
    @Html.ActionLink("Edit", "Edit", new { /* id=Model.PrimaryKey */ }) |
    @Html.ActionLink("Back to List", "Index")
</p>
